# Codex Instructions

- Always read `CODEX.md` and `MEMORY.md` at the start of a session.
- Prefer `rg` for search when looking for files or text.
- Keep edits ASCII unless a file already uses non-ASCII.
- Avoid changes in `vendor/` and `www/js/ckeditor/` unless explicitly requested.
- Update `CHANGELOG.md` and `MEMORY.md` when you add or change functionality.
- Ask before destructive operations or major design decisions.
